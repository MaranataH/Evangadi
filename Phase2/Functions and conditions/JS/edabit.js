// alert("hi its me  Mariti Konjo");

// Create a function that takes a number as an argument, increments the number by +1 and returns the result.

function incrementsBy1(parm) {
console.log( parm = parm + 1  );
    
}

incrementsBy1(0)
incrementsBy1(9)
incrementsBy1(-3)

// Create a functiothat takes two numbers as arguments and returns their sum.


arguments = (a,b) =>{
    return    a+b
}
console.log(arguments(3,5));  
console.log(arguments(-3,-6));
console.log(arguments(7,10));

 
console.log(arguments(5,6)); 



 function arguments(a,b) {
    return a+b
    
}

// Create a function that takes the age in years and returns the age in days.

function ageYearDays(age) {

    return age*365
    
}
console.log(ageYearDays(28));
console.log(ageYearDays(72));
console.log(ageYearDays(65));
console.log(ageYearDays(1));
console.log(ageYearDays(3));
console.log(ageYearDays(26));
console.log(ageYearDays(47));


// Create a function that takes voltage and current and returns the calculated power.

power = (i,v) => {
    return i*v

}
console.log(power(3,3));

function power(i,v) {
    return i*v
    
}

console.log(power(4,2));


// Write a function that takes a number and returns the perimeter of either a circle or a square. The input will be in the form (letter l, number num) where the letter will be either "s" for square, or "c" for circle, and the number will be the side of the square or the radius of the circle.

function perimeterCircleSquare (l,num) {
    let s =square
    let c= circle
    let a = side
    let b = radius
   
        return (4*(l=='s')+6.28*(l=='c'))*num
    }
    


    Array.isArray

    for (let index = 0; index < array.length; index++) {
        const element = array[index];
        
    }
 
    array.forEach(element => {
        
    });
